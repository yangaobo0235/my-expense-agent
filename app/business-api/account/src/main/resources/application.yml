server:
  port: ${ACCOUNT_PORT:25102}
spring:
  config:
    import:
      - optional:file:.env.local[.properties]
      - optional:file:../../../.env.local[.properties]
  application:
    name: campus-fund-applicant-context
  datasource:
    url: ${EXPENSE_DATASOURCE_URL}
    username: ${EXPENSE_DATASOURCE_USERNAME}
    password: ${EXPENSE_DATASOURCE_PASSWORD}
    hikari:
      maximum-pool-size: ${ACCOUNT_DATASOURCE_POOL_SIZE:5}
  flyway:
    enabled: true
    locations: classpath:db/migration
    table: flyway_schema_history_applicant_context
    baseline-on-migrate: true
    baseline-version: 0
expense:
  mcp:
    security:
      issuer-uri: ${KEYCLOAK_ISSUER_URI:http://localhost:18080/realms/campus-fund-flow}
      jwk-set-uri: ${KEYCLOAK_JWK_SET_URI:http://localhost:18080/realms/campus-fund-flow/protocol/openid-connect/certs}
      jwt-clock-skew: ${KEYCLOAK_JWT_CLOCK_SKEW:60s}
      audiences:
        - ${ACCOUNT_MCP_AUDIENCE:account-mcp}
management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  endpoint:
    health:
      probes:
        enabled: true
