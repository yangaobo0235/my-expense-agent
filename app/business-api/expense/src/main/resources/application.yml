server:
  port: ${EXPENSE_SERVICE_PORT:25103}
  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true
      force-response: true
spring:
  config:
    import:
      - optional:file:.env.local[.properties]
      - optional:file:../../../.env.local[.properties]
  application:
    name: fund-reimbursement
  datasource:
    url: ${EXPENSE_DATASOURCE_URL}
    username: ${EXPENSE_DATASOURCE_USERNAME}
    password: ${EXPENSE_DATASOURCE_PASSWORD}
    hikari:
      maximum-pool-size: ${EXPENSE_SERVICE_DATASOURCE_POOL_SIZE:5}
  flyway:
    enabled: true
    locations: classpath:db/migration
    table: flyway_schema_history_fund_reimbursement
    baseline-on-migrate: true
    baseline-version: 0
expense:
  mcp:
    security:
      service-token: ${EXPENSE_MCP_SERVICE_TOKEN:}
management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      probes:
        enabled: true
